    let esum=0;

        for(let i=0;i<nums.length;i++){
            let s=nums[i].toString()
        }
            esum=esum+nums[i];
                for(let j=0;j<s.length;j++){
                    dsum=dsum+parseInt(s[j])
                }

    
        let res=esum-dsum;
        return Math.abs(res)
};
    let dsum=0;
[
